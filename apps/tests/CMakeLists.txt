cmake_minimum_required(VERSION 2.8.9)

if(NOT BUILD_FOR_SIMULATION)
    add_subdirectory( telsdk_console_app )
if (TELSDK_FEATURE_SENSOR)
    add_subdirectory( sensor_test_app )
endif (TELSDK_FEATURE_SENSOR)
if (TELSDK_FEATURE_THERM)
    add_subdirectory( thermal_test_app )
    add_subdirectory( thermal_shutdown_test_app )
endif (TELSDK_FEATURE_THERM)
if (TELSDK_FEATURE_LOC)
    add_subdirectory( location_test_app )
endif (TELSDK_FEATURE_LOC)
if (TELSDK_FEATURE_CONFIG)
    add_subdirectory( modem_configurator )
    add_subdirectory( param_configurator )
endif (TELSDK_FEATURE_CONFIG)
if (TELSDK_FEATURE_DATA)
    add_subdirectory( data_qos_test_app )
endif (TELSDK_FEATURE_DATA)
if (TELSDK_FEATURE_PLATFORM)
    add_subdirectory( filesystem_test_app )
    add_subdirectory( telux_version_app )
    add_subdirectory( imei_test_app )
endif (TELSDK_FEATURE_PLATFORM)
if (TELSDK_FEATURE_WIFI)
    add_subdirectory( wlan_test_app )
endif (TELSDK_FEATURE_WIFI)
if (TELSDK_FEATURE_CRYPTO_ACCELERATOR)
    #add_subdirectory( crypto_console_app )
    add_subdirectory( crypto_accelerator_app )
endif (TELSDK_FEATURE_CRYPTO_ACCELERATOR)
if (TELSDK_FEATURE_POWER)
    add_subdirectory( power_test_app )
endif (TELSDK_FEATURE_POWER)

    if(MACHINE_HAS_CV2X_ONLY OR MACHINE_HAS_CV2X)
        add_subdirectory( cv2x_tcp_test )
        add_subdirectory( cv2x_unicast_app)
        add_subdirectory( cv2x_config_app )
        add_subdirectory( cv2x_get_capabilities_app )
        add_subdirectory( cv2x_set_tx_power_app)
        add_subdirectory( cv2x_l2_id_filter_app)
        add_subdirectory( cv2x_tx_report_test_app)
        add_subdirectory( cv2x_slss_rx_app)
    endif(MACHINE_HAS_CV2X_ONLY OR MACHINE_HAS_CV2X)


if (TELSDK_FEATURE_AUDIO)
    add_subdirectory( audio_console_app )
    add_subdirectory( audio_loopback_app )
endif (TELSDK_FEATURE_AUDIO)

endif(NOT BUILD_FOR_SIMULATION)
